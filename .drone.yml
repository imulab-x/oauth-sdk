---
kind: pipeline
name: oauth-sdk-pipeline

steps:
  - name: "unit, build, package"
    image: gradle:5.0.0-jdk8
    commands:
      - sudo chmod 666 /drone/src
      - gradle test
      - gradle build
      - gradle shadowJar